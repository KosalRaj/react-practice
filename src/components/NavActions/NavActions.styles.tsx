import styled from 'styled-components'

export const Wrapper = styled.div`
  color: hsl(0, 0%, 0%);
  display: flex;
  align-items: center;

  .cart {
    position: relative;
  }
  svg {
    display: block;
  }
`;